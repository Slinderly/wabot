[phases.setup]
nixPkgs = ['nodejs', 'python3', 'python311Packages.pip', 'ffmpeg']

[phases.install]
cmds = [
    'npm install',
    'python3 -m pip install --upgrade pip',
    'python3 -m pip install yt-dlp --break-system-packages'
]

[start]
cmd = 'node server.js'
